export const metadata = {
  title: "Audit console",
  description:
    "Compliance officer console - hash chain visualization, verification, and WORM checkpoint cross-check.",
};

export default function DashboardLayout({ children }) {
  return children;
}
